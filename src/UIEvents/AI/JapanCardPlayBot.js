export function playCardAction(cardNumber) {
  switch (cardNumber) {
    case 6:
      // factor would be position of CSF fleet, e.g., if too far away this card could be useless

      return true // for now
  }
}
